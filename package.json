{
  "name": "myhumans-ios-bridge",
  "private": true,
  "description": "Tests for web/, the web half of the shell bridge. Moves to lib/native/ in the myhumansapp repo at bolt-on, at which point this file and web/ are deleted.",
  "type": "module",
  "scripts": {
    "test": "vitest run --root web --environment node"
  },
  "devDependencies": {
    "vitest": "^4.1.10"
  }
}
